[project]
name = "stockdata"
version = "0.1.0"
description = "stock data mcp"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "baostock>=0.8.9",
    "fastmcp>=3.2.0",
    "pandas>=3.0.0",
    "tabulate>=0.10.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.0",
]
